# decomd Playground

A small browser playground for trying decomd transformations.

Open `index.html` from GitHub Pages, or serve the repository root locally and visit `/playground/`.
The page imports decomd from `../src/index.js` and uses an import map for the `marked` package.
